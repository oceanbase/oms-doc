|slug|prepare-the-environment|
|---|---|

# 环境准备

您可以通过 Docker 和 k8s 两种方式部署 OceanBase 迁移服务（OceanBase Migration Service，OMS）社区版，本文为您介绍部署前的准备工作。

## 通过 OBD 部署 OMS 社区版

* 图形化界面部署：详情请参见 [部署 OMS](https://www.oceanbase.com/docs/common-obd-cn-1000000005151015)。

* 命令行部署：详情请参见 [部署 OMS](https://www.oceanbase.com/docs/common-obd-cn-1000000005151037)。

## 通过 Docker 部署 OMS 社区版

1. 安装 Docker。

   部署机需要提前安装 Docker 软件，版本要求不低于 V1.13。如果 Docker 的版本低于 V1.13，会导致各服务无法启动。

2. 准备数据库，用于存储 OMS 社区版的元信息。

   OMS 社区版运行时需要一个数据库实例，用于持久化其元信息。该数据库可以是 MySQL 或 OceanBase，建议您使用 OceanBase 社区版。

   在一个 OceanBase 集群中为 OMS 社区版创建一个 2C/4G 规格的 MySQL 兼容模式租户。请记录下该租户的连接信息，会在后续部署 OMS 社区版时使用。

   <main id="notice" type='notice'>
   <h4>注意</h4>
   <p>元信息数据库用户的密码不能为空，并且不支持特殊字符（包括换行、空格，以及 |"'`()=;/&[]{}）。</p>
   </main>

## 通过 k8s 部署 OMS 社区版

1. 安装 k8s。

   部署机需要提前搭建 k8s 集群。
